

interface UserCredentials {
  email: string;
  password: string;
}

interface User {
  id: any;
  firstName: string;
  tokenId: String;
  lastName: string;
  email: string;
  phoneNumber: string;
  birthday: Date;
}