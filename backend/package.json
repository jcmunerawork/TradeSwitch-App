{
  "name": "tradeswitch-streams-backend",
  "version": "1.0.0",
  "description": "Backend proxy para TradeLocker Streams API",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "echo 'No build step required for this backend'",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
